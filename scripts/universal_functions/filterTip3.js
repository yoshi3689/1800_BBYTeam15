       //Yoshi
       
       // every preference is limited
        // else {
        // db.collection("tips")
          // .where("categories", "==", "indoor")
          // .limit(3)
          // .get().then(allTips => {
          // console.log(allTips)
          // allTips.forEach(doc => {
          //   console.log(doc.data());
        // where("type", "==", "indoor"
            // const tip = doc.data();
            // const id = tip.id;
            // if (currentUserInfo.personalTips.find(personalTipNum => personalTipNum === id)) {
            //   const name = tip.name;
            //   const docId = doc.id;
            //   const categories = tip.categories;
            //   const type = tip.type;
            //   const time = tip.time;
            //   const image = tip.image;
        
            //   tipArrToDisplay.push({
            //     name,
            //     id,
            //     categories,
            //     type,
            //     time,
            //     image,
            //     docId
            //   });
            // }
        //   })
        
        // })
        // }